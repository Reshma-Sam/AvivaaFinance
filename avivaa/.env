# Local dev: leave this commented so it uses http://localhost:5000/api (the fallback)
# Uncomment below only for production builds / Vercel deployment
# VITE_API_URL=https://avivaafinance.onrender.com/api
